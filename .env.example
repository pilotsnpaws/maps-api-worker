# Example .env file for local development
# Copy to .env and fill in your actual credentials (never commit .env to version control)

# Google Maps API Key (REQUIRED)
# Get this from: https://console.cloud.google.com/
# Enable: Maps JavaScript API
# For production, set via: wrangler secret put GOOGLE_MAPS_API_KEY
GOOGLE_MAPS_API_KEY="your_google_maps_api_key_here"

# Database Connection (OPTIONAL - for local MySQL database)
# If not set, wrangler dev will use Hyperdrive to connect to the remote database
# This is recommended since the data is read-only
# WRANGLER_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE="mysql://your_user:your_pass@your_host:3306/your_db"

